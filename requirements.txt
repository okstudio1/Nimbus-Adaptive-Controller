# Windows-only virtual-controller drivers (skipped automatically on Linux/macOS)
#
# Do not add vgamepad here. Nimbus talks to ViGEmBus directly through
# src/padbus_client.py and imports vgamepad nowhere; the name survives only in
# a docstring, because X360Pad kept its method names. Installing it also runs
# the ViGEmBus installer, which hangs a Windows CI runner until the job times
# out, so re-adding it takes the fast suite down rather than failing loudly.
pyvjoy==1.0.1; sys_platform == "win32"

# Cross-platform core
numpy>=1.24.0
PySide6>=6.6.0
httpx>=0.27.0
keyring>=25.0.0
sentry-sdk>=2.0.0

# Packaging
pyinstaller>=6.0.0
